# TalentOS リリースノート

## v1.1.0 (2026-03-19)

### 新機能
- **スタッフマスタ: 人件費フィールド追加** — 従業員ごとの年間人件費（円）を入力・管理可能に。一覧テーブルでは万円単位で表示、ソートにも対応
- **サイドバー: バージョン表示** — サイドバー左下にアプリバージョンを表示

### バグ修正
- **従業員一覧・詳細ページ: アバター表示修正** — 画像アップロードされたアバター（base64/URL）がテキストとして表示されていた問題を修正。画像として正しく描画されるように

### データベース変更
- `employees` テーブルに `salary BIGINT NOT NULL DEFAULT 0` カラムを追加
- 既存DBへの適用: `ALTER TABLE employees ADD COLUMN salary BIGINT NOT NULL DEFAULT 0;`

---

## v1.0.0 (初回リリース)

### 機能一覧
- ダッシュボード（KPI・チャート・アラート）
- 従業員管理（検索・フィルタ・ソート・詳細表示）
- 部署管理（パフォーマンス比較・連携マップ）
- スキルマトリクス（スキル×メンバー表）
- 戦略インサイト（ギャップ分析・離職リスク）
- マスタ管理（部署・スキル・スタッフ CRUD）
- ダークモード対応
- Supabase連携（モックデータからの移行）
- 操作マニュアル
- ローディングスピナー・ページネーション
- 画像アップロード・複数選択フィルタ
